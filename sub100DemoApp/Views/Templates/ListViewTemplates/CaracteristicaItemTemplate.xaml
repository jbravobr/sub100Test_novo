<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    xmlns:local="clr-namespace:sub100DemoApp;assembly=sub100DemoApp"
    xmlns:iconize="clr-namespace:FormsPlugin.Iconize;assembly=FormsPlugin.Iconize"
    x:Class="sub100DemoApp.Views.Templates.ListViewTemplates.CaracteristicaItemTemplate">
	<ContentView.Content>
        <StackLayout Orientation="Horizontal"
                 Padding="0,10"
                 TranslationX="10">
         <iconize:IconLabel HorizontalTextAlignment="Center"
               Text="{x:Static local:FontAwesomeFont.Check}"
               TextColor="{x:Static local:Theme.AccentColor}"
               VerticalTextAlignment="Start" />
        <Label VerticalOptions="CenterAndExpand" Text="{Binding .}" />
    </StackLayout>
	</ContentView.Content>
</ContentView>